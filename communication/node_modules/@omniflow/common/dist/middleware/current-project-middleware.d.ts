import { NextFunction, Request, Response } from "express";
import { IToken } from "@omniflow/common/dist/interfaces/token.interface";
import { IProject } from "@omniflow/common/dist/interfaces/entity.interface";
export default function buildVerifyProjectMiddleware({ token, }: {
    token: IToken<IProject>;
}): (expressReq: Request, res: Response, next: NextFunction) => Promise<void>;

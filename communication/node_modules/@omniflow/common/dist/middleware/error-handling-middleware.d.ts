import { NextFunction, Request, Response } from "express";
import { ErrorHandler } from "@omniflow/common/dist/lib/error-handler";
export declare const ErrorHandlingMiddleware: (err: ErrorHandler, req: Request, res: Response, next: NextFunction) => void;
